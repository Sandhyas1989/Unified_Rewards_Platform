namespace UnifiedRewards.Domain.Enums;

/// <summary>Pay band that drives rule-based compensation (e.g. performance bonus rate).</summary>
public enum GradeBand
{
    Junior = 0,
    Mid = 1,
    Senior = 2,
    Lead = 3
}
